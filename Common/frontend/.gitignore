# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo

# tsc -b (composite) emits these from the .ts config sources — derived, not source.
vite.config.js
vite.config.d.ts
tailwind.config.js
tailwind.config.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
.env.production
.env.local
# .env.development is tracked on purpose: it holds only NON-secret Entra SPA values
# (public client / no secret). Real secrets belong in .env / .env.production (ignored).
!.env.development
